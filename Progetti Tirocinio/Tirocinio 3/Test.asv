
%%Punto 1
clc
close all
clear all

m1 = 2000;
m2 = 1500;

% caso autovalori immaginari puri
k = 300;
c = 0;

% caso autovalori complessi coniugati
c = 500;



A = [0,0,1,0 ; 0,0,0,1 ; -k/m1, k/m1, -c/m1, c/m1 ; k/m2, -k/m2, c/m2, -c/m2];

B = [0 ; 0 ; 1/m1 ; 0];

M = [0 ; 0 ; 0 ; 1/m2];

C = [1, 0, 0, 0];

